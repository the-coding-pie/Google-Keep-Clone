import React from "react"

interface Props {
    modalProps: Object
}

const NoteModal: React.FC<Props> = ({ modalProps }) => {
    return (
        <div>
            Note Modal
        </div>
    )
}

export default NoteModal
